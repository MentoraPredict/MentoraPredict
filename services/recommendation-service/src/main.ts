// ── main.ts — recommendation-service
// Skeleton only. NestJS bootstrap goes here in Sprint 1.
// Port: 3007 | RFs: RF-020,RF-022
import 'reflect-metadata';

async function bootstrap() {
  // TODO Sprint 1: const app = await NestFactory.create(AppModule);
  // app.setGlobalPrefix(FULL_PREFIX);
  // app.useGlobalPipes(new ValidationPipe({ whitelist: true }));
  // app.enableCors();
  // await app.listen(3007);
  console.log('recommendation-service skeleton — not yet implemented');
}
bootstrap();
