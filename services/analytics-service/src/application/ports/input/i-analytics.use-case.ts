// Skeleton — Input port (primary port) for analytics
// Controllers depend on this interface, not on concrete use-cases
export interface IAnalyticsUseCase {
  // TODO: define use-case method signatures
}
