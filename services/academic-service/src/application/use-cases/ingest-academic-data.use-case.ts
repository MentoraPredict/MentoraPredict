// Skeleton — ingest academic data use-case
// TODO: implement in Sprint 2
export class IngestAcademicDataUseCase {
  // constructor(private readonly ...) {}
  // async execute(dto: ...): Promise<...> {}
}
