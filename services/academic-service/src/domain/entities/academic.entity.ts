// Skeleton — Academic domain entity
// Domain layer: zero framework imports, zero decorators
// TODO: define properties and domain invariants in Sprint 1
export class AcademicEntity {
  // id: string (UUID)
  // ... domain-specific properties
}
