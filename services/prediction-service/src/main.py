# main.py — prediction-service skeleton
# FastAPI bootstrap — not yet implemented
# TODO Sprint 4:
#   from fastapi import FastAPI
#   from src.infrastructure.controllers.predict_router import router
#   app = FastAPI(title="MentoraPredict — Prediction Service", version="1.0.0")
#   app.include_router(router, prefix="/api/v1")

print("prediction-service skeleton — not yet implemented")
