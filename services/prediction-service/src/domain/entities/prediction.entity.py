# Skeleton — Prediction domain entity
# Pure Python dataclass, no FastAPI dependencies
# TODO: define fields and domain invariants
from dataclasses import dataclass

@dataclass
class PredictionEntity:
    pass  # TODO: anonymous_student_id, risk_score, risk_level, confidence_score
