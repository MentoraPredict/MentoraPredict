{
  "name": "@mentorapredict/prediction-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Run: uvicorn src.main:app --reload --port 3006'",
    "test": "echo 'Run: pytest --cov=src --cov-fail-under=60'"
  },
  "description": "prediction-service Python/FastAPI skeleton — RF-019"
}
